# Camera Demo — UI strings (https://daybrite.dev/docs/localization).

app_title = Camera Demo

permission = Permission
allow = Allow
open_settings = Open Settings
take_photo = Take photo
flip = Flip
state = Camera
photo = Photo
no_photo = None yet
photo_size = { $width } × { $height } · { $name }

# The reason the OS shows when the app asks (Day.toml [permissions]).
permission_camera = Show the camera's preview and take a photo.
